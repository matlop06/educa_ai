{
  "name": "educa-ai-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client && cp -a client/dist/. server/public/",
    "start": "npm start --prefix server"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
