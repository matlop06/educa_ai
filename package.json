{
  "name": "educa-ai-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && cp -a client/dist/. server/public/",
    "start": "npm start --prefix server"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
